import counterStore from './counter';
import main from './main';
import sku from './sku';
import global from './global';

const store = {
  counterStore,
  main,
  sku,
  global,
};

export default store;

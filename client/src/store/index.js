import main from './main';
import sku from './sku';
import global from './global';
import userInfo from './userInfo';
import pkg from './pkg';

const store = {
  main,
  sku,
  global,
  userInfo,
  pkg,
};

export default store;
